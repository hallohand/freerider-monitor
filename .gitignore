# Output-Snapshots der Monitor-Skripte (groß, nicht versioniert)
hertz-data.json
movacar-data.json

# State liegt jetzt im privaten Schwester-Repo
# hallohand/freerider-monitor-state. Lokales state/-Verzeichnis ist nur
# für Test-Läufe und gehört nicht ins Repo.
/state/
.cache/

# Tooling-Spuren (lokale Brainstorm-Server-Artefakte)
.superpowers/**/state/

# Secrets — niemals committen
.env
.env.local
.env.*.local
*.secret
*.credentials

# Lokale Claude-Code-Settings
.claude/settings.local.json

# Python
__pycache__/
*.pyc
*.pyo
