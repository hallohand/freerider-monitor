# Output-Snapshots der Monitor-Skripte (groß, nicht versioniert)
hertz-data.json
movacar-data.json

# State liegt im VPS-Layout (/var/lib/freerider/state/) und gehört
# nicht ins Repo. Lokales /state/-Verzeichnis ist nur für Test-Läufe.
/state/
.cache/

# Tooling-Spuren (lokale Brainstorm-Server-Artefakte)
.superpowers/**/state/

# Secrets — niemals committen
.env
.env.local
.env.*.local
*.secret
*.credentials

# Lokale Claude-Code-Settings
.claude/settings.local.json

# Python
__pycache__/
*.pyc
*.pyo
