# Output-Snapshots der Monitor-Skripte (groß, nicht versioniert)
hertz-data.json
movacar-data.json

# State-Persistierung — Repo-Root-State: nur known-ids.txt versionieren,
# alles andere (z.B. Test-Caches) ignorieren.
/state/*
!/state/known-ids.txt
.cache/

# Tooling-Spuren (lokale Brainstorm-Server-Artefakte)
.superpowers/**/state/

# Secrets — niemals committen
.env
.env.local
.env.*.local
*.secret
*.credentials

# Lokale Claude-Code-Settings
.claude/settings.local.json

# Python
__pycache__/
*.pyc
*.pyo
